{
  "name": "yeno-os-starter",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "engines": { "node": ">=24" },
  "scripts": {
    "verify:baseline": "node scripts/verify-baseline.mjs",
    "test": "npm --prefix runtime test",
    "start": "npm --prefix runtime start --",
    "test:blackhole": "npm --prefix blackhole test"
  }
}
