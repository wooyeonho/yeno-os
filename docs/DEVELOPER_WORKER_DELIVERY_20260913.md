# BLACKHOLE 개발 실행자 — 2026-09-13 인계

## 완료 범위

실제 소스는 `blackhole/developer-worker-20260913` 브랜치, PR #2에 저장했다. 검사한 코드 커밋은 `b7feb5d4857d219d15d9c3c32238a9ed513cbc82`다. 임시 소스 전달용 파일과 워크플로는 최종 코드 트리에서 제거했다. 이 인계 문서의 후속 커밋은 검사된 코드와 구분한다.

저장소 사본 읽기 → 지정 파일의 수정안 작성 → 원래 시험 실행 → 실패 시 한 번 보완 → 수정 후보 보관 → 선택적으로 초안 PR → 정확한 커밋 승인 후 배포 요청을 구현했다. 호출은 기존 코어의 기록·한도를 사용하며 작업당 최대 2회다. 모델은 시험·인증·예산·저장소·실행자·배포 설정을 수정할 수 없도록 경로 계약을 제한한다. 실제 실행에는 네트워크 차단·읽기 전용·비관리자 Docker를 사용한다. 중단·불명확한 결과·중복 요청은 기록을 남기고 재조정 대상으로 보류한다.

한 작업의 자동 수정은 지정한 기존 코드 파일 최대 4개, 모델에 제공하는 소스는 12 KB로 제한한다. 저장소 전체 무제한 수정이나 스스로 목표를 선정하는 상주 개발 서비스가 완성됐다는 뜻은 아니다. 시험 통과는 모든 기능·보안 문제 부재를 보증하지 않으며 사람의 코드 검토를 대체하지 않는다.

## 실제 검증

[GitHub Actions 34748866363](https://github.com/wooyeonho/yeno-os/actions/runs/34748866363)에서 Node 24로 검사했다. 코어·컨트롤러·네이티브 계약 시험 459개와 개발 실행자 시험 24개, 합계 **483개 통과·실패 0·생략 0**다. 별도 웹 접속/재접속과 연구 결과 흐름 검사 스크립트 2개도 통과했다.

실제 Docker에서 실패한 코드 → 시험 실패 → 보완 → 재시험 통과, 원본·시험 보존, 비밀 환경변수와 Git 메타데이터 부재, 읽기 전용·비관리자 실행, 시간 초과 종료를 검증했다. 실제 임시 HTTP 코어와 CoreGateway를 연결한 왕복도 포함한다. 모델 공급자 응답과 GitHub 배포·복구 응답은 합성 시험 응답이며, 웹 DOM/쿠키는 JSDOM 모의 환경이다. 물리 휴대폰 검증이 아니다. 이번 작업의 실제 유료 모델 호출은 0회다.

로그 artifact `10315047244`의 ZIP을 내려받고 SHA-256 `c82f7753654fbffa75dddafcbb5efb999632e64ba71a13e945e7cccb56657584`를 대조했다. 상세 수치·로그 해시·이미지 ID는 [검증 기록](DEVELOPER_WORKER_VERIFICATION_20260913.json)에 있다. 이전 로컬 Node 22 검사 47통과/3생략을 최종 원격 결과와 합산하지 않는다.

## 운영에 남은 연결

새 코어 API는 아직 Koyeb 운영본에 배포하지 않았고, 별도 개발 호스트의 상주 실행·기기 인증·저장소 쓰기 인증을 연결하지 않았다. GitHub 연결 권한은 운영 서버의 실행자에게 자동으로 전달되지 않는다. 따라서 현재 폰에서 이 새 저장소 개발 실행자를 바로 사용할 수 있다고 표시하면 안 된다. 기존의 제한된 JavaScript 코드 작성/흡수 기능과 구분한다.

이번 작업은 `codex`와 `yeno-koyeb-pilot`을 변경하지 않았다. 확인한 개발 기준은 `8ac5ed42874abe1a6128a51077a373595e7e81e6`, 운영 브랜치는 `0007b3d143a1bacc8b8481478e5dca778f5f17ae`다. 기존 운영 데이터·인증·호출 상한도 바꾸지 않았다. 배포 승인은 시험한 기준 트리와 운영 기준 트리가 다르면 거절하도록 구현돼 있으므로 두 브랜치를 먼저 비교·조정해야 한다.

다음 실행자는 [구현 계약](DEVELOPER_WORKER_20260913.md), 이 PR의 실제 변경과 검사 기록을 읽고, 검증된 코어 API의 운영 반영 및 별도 격리 호스트 연결을 완료한 뒤 실제 공급자 1작업·초안 PR·중단/재접속을 검증한다. 코드만 반영한 상태를 상주 자율 개발 또는 운영 배포 성공으로 보고하지 않는다. 비밀값은 채팅이나 저장소에 복사하지 않는다.
