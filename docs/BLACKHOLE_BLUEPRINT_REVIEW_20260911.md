# 사용자 첨부 청사진 검토·반영 결정

검토일: 2026-09-11. 대상: 사용자 첨부 `Black_Hole_OS_Blueprint.md` 전체 563행, 45,317 bytes.

원본 SHA-256: `0c58e8b36635b4faf3c986bcdf37ed56809bb1d6ac80a44905f18292622d03db`.

`readingStatus=read`, `decision=candidate`. 이 판정은 설계 자료의 검토와 채택 결정이다. 코어 자료 등록·기능 구현·시험 통과·배포를 의미하지 않는다. 원본은 수정하지 않았다. 문서에 포함된 모든 외부 링크를 읽었다는 의미도 아니다.

## 판단

첨부안의 강점은 승인 대상을 정확한 내용에 묶는 규칙, 서비스 간 상태 소유권, 부모 예산을 공유하는 자식 작업, 승인 대기 중 자원 반납, 최종 artifact 재검사다. 기존 설계에 이를 추가한다. 현재 실행 코어를 다른 언어·앱으로 다시 쓰는 전제는 채택하지 않는다.

기준 상태는 저장소 `b17fb847c48d37e9ac7ed97e306a24516c9e38d4`의 STATUS를 다시 읽어 확인했다. 해당 기록상 운영 소스는 `5c361fe8`, AI 요청 수정은 `6762a627` 검사164개 통과·배포 대기·실제 모델 미연결이다. 이번 검토에서 서버 상태를 새로 호출하거나 164개 검사를 재실행한 것은 아니다.

## 반영할 명세

| 항목 | 기존 설계에 추가할 구체적인 요구 | 구현 완료 판정 |
|---|---|---|
| 정확한 승인 대상 | Approval에 owner, action/tool/payload digest, target, 비용 상한, 만료, policy revision을 묶는다. 단발 승인은 정확히 같은 내용에만 적용한다. 유효한 상시 승인은 위임한 범위 안에서 재사용한다 | 승인 후 내용·계정·대상·도구를 바꾸면 실행 거절. 범위가 같은 상시 승인에는 재질문 없음 |
| 부모 예산·분할 한도 | 목표 분해에도 부모의 총 비용·시간·호출 한도를 공유한다. 자식에게 부모보다 큰 권한을 줄 수 없다. 깊이3·직접 자식5개는 제안값이며 기존 운영값을 바꾸지 않는다 | 여러 자식이 동시에 마지막 예산을 예약해도 원장 불변식 유지. 분할로 비용·권한 제한 우회 불가 |
| Outbox와 Inbox | 발신 상태 변경과 outbox는 같은 서비스 DB 트랜잭션. 수신 처리와 inbox event_id 기록도 원자적으로 묶는다. 외부 효과는 operation_id와 별도 결과 조회로 처리한다 | 동일 event_id 100회 전달에도 의도·목표가 중복 생성되지 않음. 성공 직후 worker 종료 시 외부 결과 조정 |
| 승인 대기 자원 반납 | WAITING_APPROVAL에는 영속 상태와 미리보기만 보존한다. worker slot·VM은 반환하고 승인 후 같은 작업으로 재개한다 | 승인 대기 중 VM 과금·슬롯 점유 종료 확인. 무응답은 동의로 처리하지 않음 |
| 최종 빌드 재검사 | 소스 사전 검사와 별도로 최종 이미지·실행 파일의 전이 의존성·라이선스·취약점 검사. 스키마와 wrapper도 검사 대상 digest에 포함한다 | 소스가 허용돼도 최종 결과에 금지 의존성이 있으면 승격 거절. 검사 이후 바이너리 변경 시 증거 무효 |
| 등록과 활성화 분리 | Staged 등록 성공과 Active 실행 허가를 별도 상태로 둔다. 등록 이벤트 유실은 outbox로 복구한다 | 등록만 된 도구는 실행 불가. 활성화 시 현재 권한·정확한 digest·평가 유효기간 재확인 |
| 원격 MCP 신뢰 한계 | endpoint·audience·schema·권한을 고정해도 원격 구현 자체를 고정한 것으로 취급하지 않는다. 매 호출 scope·데이터 목적지·출력을 검증한다 | 원격 명세 변경·권한 확대·허가되지 않은 목적지 요청 시 보류 또는 회수 |
| 신호 품질 | 같은 보도자료의 복제는 한 근거로 묶고 precision@K·불필요한 알림·실측 순효익을 관찰한다 | 후보 수 증가만으로 성과를 보고하지 않음. 실제 유용성 평가에 미채택 표본도 포함 |

LangGraph는 interrupt에서 재개할 때 해당 노드를 처음부터 다시 실행할 수 있다. 승인 요청·실제 외부 실행 노드를 분리하고, 승인 전 코드도 반복 실행을 견디게 한다. [공식 Interrupts 문서](https://docs.langchain.com/oss/python/langgraph/interrupts), 2026-09-11 확인.

## 현재 제품에 맞춘 수정·보류

| 첨부안 | BLACKHOLE 적용 결정 | 이유 |
|---|---|---|
| Python/FastAPI로 서비스 전체 시작 | 기존 Node24/TypeScript 유지. Python은 특정 작업자의 라이브러리가 필요할 때 사용 | 현재 코어·인증·작업·검사 자산을 살림 |
| PWA를 기본 사용자 인터페이스로 선택 | 기존 Tauri APK·Windows EXE 목표 유지. 웹은 보조 접근 경로 | 사용자가 요구한 설치형 앱·기기 보관소·현재 연결 경로 보존 |
| Phase1에 모델2개·5서비스·격리 흡수·서명 전체를 포함 | 첫 실제 모델→기존 프로젝트 실행→폰 조회→중단·재시작을 먼저 완료. 복수 모델·서비스 분리는 그 위에 확장 | 최초 실사용 경로를 큰 인프라 구축의 완료까지 기다리게 하지 않음 |
| Temporal Phase2 도입 | 장기 타이머·취소·다중 호스트 복구의 실제 필요가 확인될 때 검토 | 큐·LangGraph·Temporal의 중복 재시도와 운영 부담 방지 |
| ORT·ScanCode·Syft·Grype·Cosign 전체 동시 도입 | 필요한 검사·서명 계약은 채택. 구체 도구는 중복·자원·라이선스 평가 후 순차 선택 | 도구 설치 수를 기능 완료로 세지 않음 |
| 작업당 모델20회·월20만원 예시 | 기존 운영 한도 유지. 설계 제안은 모델8턴·실행2개를 기본으로 검토, 실제 금액은 승인된 예산 적용 | 첨부의 예시값은 운영 설정 변경 승인이 아님 |
| 48개 논리 역할 예시 | 원본49개 프로젝트와 별도 V11의 기존 ID·목적 유지 | 역할 수와 원본 프로젝트 수를 혼동하지 않음 |

## 출처별 접근 조건 확인

Product Hunt API 공식 문서는 기본 상업 이용을 허용하지 않고 사업 이용 시 문의하도록 명시한다. 사업 목적의 자동 수집 어댑터 활성화 전 허용 범위를 확인한다. [Product Hunt API](https://api.producthunt.com/v2/docs), 2026-09-11 확인.

Reddit Data API 약관은 상업 목적 등에 별도 계약을 요구하며 승인된 용도 밖의 보존·이용을 제한한다. 연결 시 계정의 실제 계약·용도·삭제 조건을 적용한다. [Reddit Data API Terms](https://redditinc.com/policies/data-api-terms), 2026-09-11 확인.

위 조건은 해당 API에 대한 확인이다. 모든 공개 웹 페이지 접근을 같은 조항으로 단정하거나 다른 수집 방식으로 제한을 우회하지 않는다. 이번에는 외부 서비스 가입·연락·계약·게시·결제·수집 활성화를 수행하지 않았다.

## 다음 구현과의 연결

현재 모델 연결과 AI 수정본 운영 반영을 먼저 완성한다. 이후 실제 개발 실행자를 연결할 때 이 문서의 승인·부모 예산·중복·최종 빌드 검사 계약을 함께 구현한다. 그 전에 무인 개발이 가능하거나 첨부안의 인수 검증을 통과했다고 보고하지 않는다.

검토 결과는 [BLACKHOLE_ARCHITECTURE.md](BLACKHOLE_ARCHITECTURE.md)의 보완 명세로 연결한다. 신규 프로젝트를 생성하거나 원본 프로젝트를 대체하지 않는다.
