# 최신 배포와 재시작 보존 확인 — 2026-09-10

크기 초과 시 한 번만 latest를 조회하는 보완 소스 `a6382264e94f56ffd87b2c12c3897354d663281a`를 기존 Koyeb 서버에 반영했다. 배포 `9b0191bf-284b-4a6c-b3e9-b06395146d2e`가 **Healthy / Active deployment**이며, 화면의 커밋과 일치한다. 전체 검사 115/115 통과.

02:42:45Z HTTPS 재검사에서 revision 125, 수집 enabled=true, 기존 20개 프로젝트·79개 자료·11개 작업, 다음 시각 **2026-09-11 02:37:07.182Z**를 확인했다. 새 인스턴스에서도 기존 원본 행과 같은 결과 파일/해시가 유지되고 즉시 재수집·중복 추가하지 않았다. 마지막 실제 수집은 아래의 최초 소스에서 실행한 partial/신규 3개 기록이다. 새 fallback의 실제 Koyeb 외부 호출은 아직 다음 정기 실행 전이며 성공으로 소급 변경하지 않았다. 새 APK 설치나 실제 AI/개발 실행자 연결을 이번 배포로 완료 처리하지 않는다.

# 2026-09-10 상주 자료 확인 실제 가동

소스 `668ad18b77cb4dbafc81ab7cc23b1fa5517c0ae8`, Koyeb 배포 `1e70e955-7e65-4a75-9c91-09be3be9cc05`가 Healthy/1개 인스턴스로 전환됐다. 이전 배포는 Stopped다. 이미지 manifest `1d94d50faedc79e2896edf85197e0e517d7270c038533a120e5902456eb7ce59`.

2026-09-10 02:37:07.182Z, 인증된 설정 활성화 후 코어의 스케줄러가 직접 실행했다. 02:37:09.996Z 완료, **partial**: Claude Code 2개·Gemini CLI 1개를 unread/pending으로 접수했다. Tauri·xAI 목록 조회는 성공/신규 0개, Codex 목록은 2MiB 제한 초과로 미접수다. 5개 전부 수집 성공이라고 해석하지 않는다. 다음 확인 2026-09-11 02:37:07.182Z(한국 시각 11:37:07).

실제 입력: [Claude v2.1.267](https://github.com/anthropics/claude-code/releases/tag/v2.1.267), [Claude v2.1.266](https://github.com/anthropics/claude-code/releases/tag/v2.1.266), [Gemini CLI v0.59.0](https://github.com/google-gemini/gemini-cli/releases/tag/v0.59.0). 원문 읽음/후보 채택/코드 변경으로 표시하지 않았다. 기존 20개 프로젝트·76개 자료·10개 작업·기억·스냅샷 원본 행의 보존을 비교 확인했다.

02:40:03Z 실제 상태 revision 123, 자료 79개, 작업 11개. 기존 APK가 쓸 수 있는 `자율 점검` 명령으로 작업 `4794a1ac-f5f8-4269-b846-bf83ac570ff4`와 결과 `527c3aba-68a5-4e9f-8931-5fe6024732b9`를 생성·다운로드했다. 결과 1,507 bytes / SHA-256 `690591e116d11b796e5d1fa41e650d446c0466bb4155c0299d091242635ef08d`, 서버 해시와 일치했다. 이는 실제 HTTPS API 검사이며 폰의 화면 조작 시험은 아니다.

실제 발견한 Codex 큰 목록 문제를 보완했다. 같은 시간/바이트 제한을 유지하면서 latest endpoint를 한 번만 조회한다. 원자료 API의 최신 정식 릴리스 응답은 292,406자였으며 이 조회 자체는 연결된 GitHub를 통한 확인이다. 보완 코드의 로컬 전체 검사 **115/115 통과**, 0 실패/취소/생략, Node 24, 16.976초. 실제 상주 코어의 fallback 경로 성공은 후속 예약 실행에서 확인할 항목이며 지금 성공으로 표시하지 않는다.

ChatGPT 예약과 독립적인 **공식 자료 메타데이터 접수**만 자율 가동한다. 실제 AI 호출은 configured=false, 개발 실행자 false. 일반 웹 검색·원문 판단·코드 수정·시험·배포의 자율 개선 고리는 미연결이다. [범위와 제어](SOURCE_WATCH.md).

# YENO 실제 운영 검사 — 2026-09-10

## 최신: 요청 원장 배포와 실제 HTTPS 재시도 검증

실행 소스 `9dae494a91576547fa7798916e07a0a53eb8324d`, 코어0.2.2/API1. Koyeb 배포 `0ca03332-687a-414c-a655-93f8e8c6dfb8`의 Docker 빌드 성공·Healthy·1 of 1 running과 이전 `5d112533` Stopped를 확인했다. 실행 이미지 manifest SHA-256 `8940f68609b54e92040e4d0cfb89011afac92022bf5dc243ca6d3d5fdcd89af7`.

- Node24.19.0 전체107/107 통과, 실패·취소·건너뜀0,17.005초. controller TypeScript/Vite 빌드 성공. 이전 결과와 합산하지 않으며 새 APK 생성은 아니다.
- 단위/실제 HTTP 검사: 응답캐시2,000건/2MiB 정리·별도20,000건 원장·캐시없는 동일작업 복구·새 프로세스 재시작·암호화 백업 새 경로 복원·용량에서 새작업507/멈춤·폐기 허용. 교차기기 자기폐기 요청 재사용은409로 거부한다. 용량 fixture는 정지된 상태에 준비한 합법 원장이며20,000회 실사용 부하시험이 아니다.
- 운영 HTTPS 완료 `2026-09-10T01:49:17.065911+00:00`. 배포 전 revision99의 프로젝트20·자료76·작업9·기억0·스냅샷을 전체 필드 비교로 보존 확인했다. 현재 revision108, 작업10개. 추가된1개는 이번 검증 문서다.
- 실제 APK와 같은 `Origin: http://tauri.localhost` 및 기기 bearer로 명령을 처리했다. ID없는 명령/등록400, 같은 요청201·동일job, 다른본문409, 인증된 요청 참조조회200을 확인했다.
- **YENO 재시도 보호 검증** 내용의 실제 문서: job `951b1758-78cc-47b7-a896-c4a83f2c9282`, artifact `735f272a-7ec7-4296-8cb3-6b100a50f18e`, 2329bytes, SHA-256 `ad3a03212967d238ab5cc0e1ff1c3190dac3cc3ea784412a5aec1d18b58f0c8d`. 다운로드 바이트와 서버 해시 헤더가 일치했다. 같은 요청 재전송 후에도 작업이 정확히1개다.
- 이번 시험기기만 자기폐기200, 이후 상태401. 소유자 폰 인증을 변경하지 않았다. AI/무인 개발 작업자 false 유지, 유료 모델 호출 없음.

앞선 결과 회수는 사용량 한도로 자동 승인 검토에서 막혔고 이 시점에 정상 실행 도구로 재개했다. 중단 전에 저장한 요청을 먼저 조회하고 같은 ID로 검증했으며 새 문서를 중복 생성하지 않았다. 요청 조회 중 전송 시간 초과1회는 동일 요청 재시도로 완료했다.

**남은 부분:** native 소스의401/403/410 대기ID 보완은 새 APK 빌드·설치 전까지 폰에 미적용이다. 폰 결과 열기·종료/재접속은 실기기 미확인. 이 환경에는 Android SDK/NDK·Rust가 없고 GitHub workflow dispatch 도구가 노출되지 않아 새 빌드를 시작하지 못했다. 기존 수동 빌드는 유지한다. 현재 Codex가 직접 개발·디자인·검증을 맡으며 별도 AI 실행자 연결을 현 개발 선행 조건으로 요구하지 않는다. 백업 장기보관 전송 실패도 해결됐다고 표시하지 않는다.

## 이전: 암호화 전체 백업·새 프로세스 복원·소유자 기기 폐기

실행 소스 `3757f86f5287ffda3397a05a17bb96c876d9c464`, 코어 0.2.2 / API 1. Koyeb 배포 `5d112533-304a-4d6b-8a4d-6bd9147b5926`의 Docker 빌드 성공·Healthy·이전 `7ea4c124` Stopped·실행 1개를 확인했다. 이미지 manifest SHA-256 `175e96fe6149fc7b02ae2f344ac35d7247d15e8b12059e35be5533edff7cb4fa`.

- 최종 Node 24.19.0 전체 검사 **87/87 통과**, 실패·취소·건너뜀 0, 16.400초. 기존 71개에 백업 단위 검사 11개·기기 관리 3개·HTTP 복원/기기 폐기 2개를 추가했다.
- 로컬 검사는 잘못된 키·변조·잘린 파일·잘못된 참조/경로·링크·용량 초과·기존 대상 덮어쓰기 거부와 write/fsync/삭제 실패 주입을 포함한다. 복원 표식이 남으면 정상·불완전 상태 모두 시작을 거부한다.
- 배포 전 `2026-09-09T22:31:40.536800+00:00`, revision 88 상태에서 프로젝트 20개·자료 76개·작업 8개·기억 0개와 스냅샷의 전체 필드 지문을 기록했다. 새 배포 후 동일 필드 지문을 확인했다.
- 실제 HTTPS 관리자 기기 목록은 비밀 필드를 반환하지 않았다. 기기 bearer의 전체 백업은 403, 소유자의 대상 폐기는 200, 같은 요청은 같은 영수증, 폐기 토큰은 401이었다. 시험 기기 2개만 폐기했다. 기존 기기의 ID·이름·플랫폼·생성/폐기 시각은 그대로 보존됐다.
- 암호화 백업 생성 시각 `2026-09-09T22:45:27.828Z`. **335,792 bytes**, SHA-256 `9272c9ed43d27c385301e2cc87049b686340da48a2d63a43009c87526cb215e2`. 다운로드 해시와 인증 복호화를 검증했다. 페어링 원문 키와 환경 변수는 포함하지 않았다.
- 별도 파일시스템의 새 디렉터리에 복원하고 **실제 새 Node 프로세스**를 loopback에서 실행했다. 프로젝트 20개·자료 76개·작업 8개·결과 8개·기억 0개와 요청/스냅샷/결과 메타데이터를 일치 비교했다. 모든 결과의 실제 다운로드 바이트·해시가 같았고, 기존 운영 브리핑 요청 ID는 같은 영수증을 반환하며 새 작업을 만들지 않았다.
- 복원 사본은 전체 멈춤 true·AI false, 기존 활성 기기 2개 폐기였다. 옛 토큰은 401, 새로운 소유자 키로 등록한 새 시험 기기는 인증에 성공했다. 완료 시각 `2026-09-09T22:46:00.970Z`. 이 작은 데이터의 로컬 복원+API 확인 0.164초는 운영 호스트 전체 복구 시간 보장이 아니다. 확인 후 시험 프로세스를 종료했다. 운영 코어의 주소·연결 키·소유자 폰은 교체하지 않았다.
- 최종 HTTPS 보존·폐기 확인 완료 `2026-09-09T22:46:37.113669+00:00`. 개발 작업자와 유료 AI 호출은 없다. APK/EXE를 새로 빌드한 것이 아니다.

검증 과정에서 관리 화면의 입력 연결이 멈춰 같은 브라우저의 새 탭으로 다시 연결한 후 정상 배포했다. 최초 HTTPS 검증 보조 스크립트는 헤더 이름의 대소문자 처리와 재실행 시 시험 기기 비교 오류로 실패했다. 이를 분리해 수정하고 실제 데이터·기존 기기·복원 검사를 완료했다. 실패한 검사를 통과 건수에 합산하지 않았다.

**남은 저장 실패:** 위 암호화 파일과 복구 키의 지속 보관 전송을 두 차례 시도했으나 두 파일 모두 `transfer_failed`였다. 외부 지속 저장 성공으로 표시하지 않는다. 다른 환경에서 복원 가능한 파일과 코드 경로는 검증했지만, 다운로드용 임시 사본을 장기 보관 백업으로 간주하지 않는다. 정기 독립 백업 운영·보관 사본 재회수·운영 호스트 교체 복구는 남아 있다. 소유자에게 저장 실패와 두 다운로드용 파일을 구분해 알린다.

검사 후 기존 코어에 **YENO 출시 기준 점검** 문서를 생성했다. 작업 `c9000433-31c4-46c5-b016-2824c8404f51`, 결과 `8de4a2ba-81a4-432d-b713-2547b562f160`, 4325 bytes, SHA-256 `25480abbf3e68345b17508263b7c29f4160c5cd617f3b47253ab218237161187`. 실제 다운로드와 해시를 `2026-09-09T22:51:51.785730+00:00`에 확인했다. 접수 전송 시간 초과 뒤 같은 저장된 요청 ID로 재시도했다. 이 문서 추가 후 작업은 9개이며 위 백업은 그 전에 생성한 8개 작업의 사본이다. 폰에서 문서를 열었다는 사용자 확인은 아직 없다.

## 이전: 운영 브리핑과 첫 운영 결과 회수

실행 소스 `5f80670df62ee2900b57f2ce7e4f66d4d2d2f69b`, 코어 0.2.2 / API 1. Koyeb 배포 `7ea4c124-e1e3-482e-bb3a-1de47bf64aa4`의 Docker 빌드 성공·Healthy·이전 `1946096b` Stopped를 확인했다. 이미지 manifest SHA-256 `74600694ec947092c686303a75b166c69f296f18ed1ed54adf1074fe44a46c26`.

- Node 24.19.0 전체 검사 **71/71 통과**, 실패·취소·건너뜀 0, 16.179초. 새 운영 문서의 native 명령·동일 요청·실제 파일·서버 재시작 보존 포함.
- 소유자 Android 화면에서 `코어 응답 확인됨`, revision 73을 확인했다. 최초 연결·상태 조회의 실기기 증거이며 폰 명령·결과 열기·종료/재실행의 증거는 아니다.
- 실제 HTTPS에서 시험 기기 등록 후 yeno-os의 `nextAction`만 갱신했다(프로젝트 버전 2). 나머지 19개 프로젝트와 대상 프로젝트의 다른 필드는 보존했다.
- **YENO 첫 운영 방향**: 작업 `7ae6685c-5c38-4e71-8fc4-9629a15fa8ae`, 결과 `1848805b-ad4a-493f-b87b-509d1fd02008`, 6588 bytes, SHA-256 `2743b1e2ede5b860d0e516d987f04c1edd533a1470ceead24dad8edb75e4db41`.
- **YENO 운영 브리핑**: 작업 `0657bd06-d50f-4061-a24a-367a30ba456a`, 결과 `d1487164-80b9-4e0d-af61-2d0a2c1f265c`, 3939 bytes, SHA-256 `a3f0e7cb494e8fc1140947c8819c2590dc6e60ef12e304077be26fb84664c06b`.
- 두 실제 다운로드와 서버 해시 헤더가 일치했다. 같은 브리핑 요청 재전송은 같은 작업을 반환했다. 운영 명령 수행 전후 자료 76개·기억·기존 작업 6개를 보존했고 시험 기기만 폐기한 뒤 401을 확인했다.
- 확인 완료 `2026-09-09T21:43:55.491989Z`. AI 설정 false, developerWorker false. 이 세션의 구현·검사·배포와 코어의 보고서 생성을 구분한다. 상주 자율 개발, 신규 고객·매출, Windows EXE를 검증하거나 활성화한 것은 아니다.

원격 브랜치 반영 전 상태를 근거로 첫 재배포 준비 동작이 자동 검토에서 거절됐다. 이후 GitHub 양쪽 브랜치의 실제 새 커밋을 다시 확인한 뒤 같은 배포 경로가 허용되어 완료했다. 거절된 동작을 우회하는 별도 경로는 사용하지 않았다.

## 이전: Android Origin 호환성 수정

실행 소스 `5a173ea69b6b9d32b01458330991d1c9f3b2bb8f`, 코어 0.2.2 / API 1. 배포 `1946096b-b35a-4ab4-8588-a98be088b3d7`의 Docker 빌드 성공·Healthy·이전 실행 Stopped를 확인했다.

- 전체 Node 24.19.0 검사 **66/66 통과**, 실패·취소·건너뜀 0, 14.668초.
- 실제 APK의 `Origin: http://tauri.localhost`로 health·등록·기기 상태·문서 결과·동일 요청 재전송·시험 기기 폐기 후 401을 확인했다. 검사 완료 `2026-09-09T21:19:58.470652Z`.
- 프로젝트 20개·자료 76개·기억과 기존 작업 5개 보존 확인. 새 서버 시험 문서 575 bytes와 다운로드 해시 일치.
- 키 없는 요청과 임의 외부 Origin 거부 유지. 첫 검사 환경 전송 시간 초과 뒤 재시도로 완료했으며, Android 실기기 연결 성공은 아직 미검증이다.

원인·정확한 범위·결과 해시는 [Android Origin 수정 기록](ANDROID_ORIGIN_FIX.md)을 따른다. 아래는 이전 배포 시점의 보존 검사 이력이다.

## 0.2.2 자료·프로젝트 실제 보존 검사

실행 소스 `ee473e7f6d7c8b71bd4b5dee1ea95381c969a6b8`, API `1`. 실제 Docker 빌드 배포 `010b133b-680c-42b3-8407-8b362fe8ebff`와 기존 이미지 재사용 재시작 `bd3c4123-715e-49c5-917d-e6d30113aa2b`를 Koyeb에서 확인했다. 새 실행 Healthy, 이전 실행 Stopped. 이미지 manifest SHA-256 `e64724365c5e3cd6d941142995285d2896252b6e1feebc5a34e934df00365e4f`.

- 로컬 Node 24.19.0 전체 검사 **63/63 통과**, 실패·취소·건너뜀 0, 14.411초. 자료의 실제 HTTP·SIGKILL 재시작·결과 해시와 ENOSPC 실패 주입 포함. UI 요청 흐름의 DOM/API 모의 검사는 실제 브라우저·폰 검사와 구분한다.
- 프로젝트 **20개** 등록, 업그레이드 후 모든 메타데이터 일치. 생성 영수증은 대표 **1개**를 재전송했고 프로젝트 문서 명령 **3개 모두**의 같은 작업·영수증·결과 해시를 확인했다. 확인 완료 `2026-09-09T18:10:06.312+00:00`, 새 시작 `2026-09-09T18:05:11.293Z`. 0.2.0의 기존 569-byte 문서도 같은 해시로 유지됐다.
- 자료 **76개** 실제 등록: 미확인 48, 일부 확인 14, 내용 확인 5, 접근 불가 9. 개선 후보 5, 검토 대기 71. 소셜 자료 전체 열람은 0이며, 직접 읽은 공식 문서 5개만 후보로 등록했다.
- 같은 76개 가져오기 요청을 재전송해 동일 영수증·자료 ID를 확인했다. 재시작 후 전체 자료의 모든 필드·버전·ID가 같았다. 재시작 전 `2026-09-09T18:08:57.441+00:00`, 새 시작 `2026-09-09T18:11:40.015Z`, 보존 확인 `2026-09-09T18:15:28.874+00:00`.
- 실제 개선 후보 문서: 작업 `fb4b3188-ee64-423b-b73b-862c7d2ee32e`, 결과 `45a7fc2d-98af-4bf8-bf0c-38c6249c65bb`, **2861 bytes**, SHA-256 `b932d053a82753b4f51b46f5d256dc1fcf8f961d30159cce3462ebdcc373b13d`. 재시작 전후 다운로드와 서버 해시가 같고 같은 명령은 같은 작업을 반환했다.
- 검사용 기기 두 개만 폐기했고 각각 이후 접근 401을 확인했다. 소유자 기기와 연결 키는 변경하지 않았다.

자료의 읽기 상태는 이번 조사자가 기록한 검토 상태다. 코어가 SNS 본문을 자동 조회·학습했다는 뜻이 아니다. 실제 AI 공급자, 개발 실행자, 자동 수집·후보 파일 동기화는 미연결이다. Android 실제 등록·명령·종료·재접속은 아직 미검증이며 새 APK나 Windows EXE를 빌드하지 않았다.

실제 호스트 시험은 같은 볼륨을 사용하는 정상 재배포다. 독립 백업에서 복원, 볼륨 소실, 장시간 부하·모든 장애 복구를 검증하지 않았다. 단일 writer와 고정 인스턴스 1개를 유지한다.

---

# 이전 0.2.0 검사 기록

2026-09-09 UTC. **전용 YENO 서비스의 실제 Docker 빌드, HTTPS 접속, 기기 인증, 문서 생성, 요청 중복 방지, 인스턴스 교체 후 결과 보존을 확인했다.** Android 앱에서의 등록·명령·재접속 검사는 아직 남아 있다.

## 실제 배포 대상

| 항목 | 확인 값 |
| --- | --- |
| HTTPS origin | `https://global-iris-gyeol-98386a17.koyeb.app` |
| Koyeb 서비스 ID | `7fb597dd-726e-45ee-bfde-7f695f6dc8ba` |
| 소스 커밋 접두사 | `481d6ea` |
| 빌드 파일 | `Dockerfile.koyeb` |
| 첫 정상 배포 | `153770e4-d07d-4896-8c86-96acf1f87d9a` |
| 재시작 후 정상 배포 | `86051e67-c4f7-4390-b3e8-870a8828322c` |
| 재시작 방식 | Koyeb에서 기존 빌드를 재사용한 새 배포. 이전 인스턴스는 Stopped, 새 배포는 Healthy임을 화면에서 확인 |
| 데이터 경로 | 전용 볼륨 `/var/lib/yeno` 아래 `/var/lib/yeno/data` |

공유 시작 코드가 실제 마운트·쓰기 가능 여부와 상속된 독점 flock/procfs를 검증한 뒤 상태를 열었다. 해당 검사를 생략하는 설정은 사용하지 않았다. 이미지의 `USER node:node` 실행 구성을 사용했으며, 별도로 `id`를 출력해 실제 숫자 UID를 측정한 것은 아니다. 이 문서는 원래 Gyeol 서비스의 동작 검증이나 데이터 이전을 뜻하지 않는다.

## HTTPS로 실행한 검사

재시작 전 검증 완료: **2026-09-09T12:58:56Z**.

| 요청·동작 | 실제 결과 |
| --- | --- |
| `GET /api/v1/health` | HTTP 200. `YENO OS`, 버전 `0.2.0`, API `1`, `authRequired:true` |
| 인증 없이 `GET /api/v1/state` | HTTP 401 |
| 새 시험 기기 등록 `POST /api/v1/devices/enroll` | HTTP 201. 이후 요청은 반환된 기기별 bearer로 수행 |
| 기기 인증 후 `GET /api/v1/state` | HTTP 200 |
| `POST /api/v1/commands` 문서 명령 | HTTP 201, 작업 1개 생성 후 `completed` |
| `GET /api/v1/artifacts/<id>` | HTTP 200, 실제 문서 569 bytes, 제출한 시험 문구 포함 |
| 동일한 본문·`requestId` 재전송 | HTTP 201, 같은 작업 ID 반환. 해당 작업은 상태에 정확히 1개 |
| AI 설정 | `ai.configured:false`; 이 검사에 유료 모델 호출 없음 |

시험 명령은 `문서 만들어: YENO Koyeb live deployment acceptance. This result must survive the core restart.`이며 실제 개인 데이터는 사용하지 않았다.

- 작업 ID: `fde464ee-edd1-481d-816f-95059b649e7e`
- 결과 ID: `9421557a-4fa7-4694-b950-5a6d58d74c89`
- 결과 SHA-256: `b361460ae0afa18ed51a280a5087e8d56293c925f4d26af8fa4f9fc2f4590204`
- 다운로드한 바이트의 SHA-256과 서버의 `X-Content-SHA256` 헤더가 일치했다.

## 실제 인스턴스 교체 후 검사

새 코어가 저장한 시작 이벤트: **2026-09-09T13:00:29.257Z**. 재시작 전 검사 완료 시각 이후의 이벤트임을 확인했다. 재시작 후 보존 검증 완료: **2026-09-09T13:01:51Z**.

1. 재시작 전에 저장한 동일 기기 bearer로 상태를 조회해 HTTP 200을 받았다.
2. 같은 작업 ID가 정확히 1개, `completed` 상태로 유지됐다.
3. 같은 결과 ID를 내려받아 569 bytes 및 위 SHA-256이 모두 같고 서버 헤더와도 일치함을 확인했다.
4. 원래 명령 본문과 `requestId`를 다시 보내 같은 작업 ID가 반환돼 요청 기록의 보존을 확인했다.
5. 보존 검증 후 임시 시험 기기를 폐기해 HTTP 200을 받았으며, 폐기한 bearer의 상태 조회는 HTTP 401로 거절됐다. 소유자 폰의 기기를 폐기한 것이 아니다.

페어링 키와 기기 bearer는 이 문서·소스·검사 출력에 기록하지 않았다. 시험용 연결 정보만 별도 비공개 파일에 0600 권한으로 저장했으며 임시 기기는 폐기됐다.

## 현재 증거의 범위

- APK 설치와 첫 연결 화면 표시는 소유자 캡처로 확인했다. **Android 폰에서 실제 페어링, 명령, 앱 종료·재실행, 정지·재개는 아직 미검증**이다.
- 실제 클라우드 검사는 정상 인스턴스 교체다. 클라우드에서 SIGKILL, 디스크 장애, 백업 복원, 장시간 부하를 시험한 것은 아니다. 기존 로컬 SIGKILL 시험과 구분한다.
- 실제 AI 연결, 자유 명령 도구 실행, 개발 작업자, 자동 개선 배포, Windows EXE는 이 검사로 완료되지 않는다.
- JSON 저장소와 독점 잠금은 **단일 writer**를 전제로 한다. 여러 인스턴스로 확대하지 않는다.
- Koyeb 볼륨은 공식 문서상 시험용 public preview이고 로컬 디스크의 중복 저장을 보장하지 않는다. 이번 재시작 보존 성공은 독립 백업·복원 또는 디스크 장애 보호의 증거가 아니다. 장기 개인 데이터의 유일한 보관소로 승격하기 전에 별도 백업과 복원을 검증한다. [Koyeb 볼륨 문서](https://www.koyeb.com/docs/reference/volumes)
