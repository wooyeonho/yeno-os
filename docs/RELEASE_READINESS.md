# YENO 출시 기준 점검 — 2026-09-09 UTC

판정: 개인 소유자용 초기 코어. 전체 출시 기준은 미충족이다. ‘세계 최고’라는 비교 결과나 완벽한 무장애 동작을 검증한 문서가 아니다. 아래 기준은 YENO 목표에서 도출한 내부 출시 조건이며 외부 인증을 받은 표준이 아니다.

| 기준 | 근거와 현재 판단 | 완료를 인정할 다음 증거 |
| --- | --- | --- |
| 폰에서 실제 사용 | APK 빌드·설치·최초 등록과 상태 조회 확인. 일부 통과 | 폰 명령 10건·실제 결과 열기·앱 종료/재접속 3회, 손실·중복 0 |
| 실제 실행과 정직한 상태 | 문서·기억·프로젝트·자료·운영 브리핑의 API·파일 검사. 범위 내 통과 | 일반 자연어를 도구 실행으로 연결한 실제 결과 |
| 데이터 지속성 | 단일 writer·영속 볼륨·재배포 보존과 실제 20개 프로젝트·76개 자료·8개 결과의 새 프로세스 복원 확인. 백업 파일 지속 보관 전송 실패 | 보관 오류 해결·보관 사본 재회수·정기 백업·운영 호스트 복구 시간 측정 |
| 인증·소유자 통제 | 기기 bearer·전체 멈춤. 소유자 분실 기기 원격 폐기와 복원 시 옛 인증 차단을 로컬/실제 HTTPS에서 확인 | 앱 관리 화면·분실 대응 실사용, 지속 보안 검토 |
| 개발 실행자 | 미연결. 현재 세션에서 코딩하는 것과 상주 실행자를 구분 | 자격 증명·비용 상한·격리 환경이 있는 작업자 1개로 제한된 코드 변경 10건 |
| AI 비용·외부 작업 | 실제 유료 공급자 검증 없음. 범용 루프 없음 | 비용 예약·정산·불명확 결과 처리·중단 전파를 실제 공급자에서 확인 |
| 재시도 중복 방지 | 앱은 지속 ID 사용, 저장된 영수증 재시도 통과. 일부 통과 | 명령·등록 등 버전 API의 ID 필수화, 2,000건 응답 캐시 이후에도 유지할 실행 식별자 |
| Android·Windows 배포 | Android 디버그 APK만 실제 생성. Windows 미완료 | Windows EXE 실제 빌드·설치, 양쪽 업데이트 서명·복구 검사 |
| 음성·화면·PC 파일 | 미구현 | 허용 폴더·권한에서 실제 실행·취소·결과 확인 |
| 자기 개선 | 자료 검토·후보 문서까지. 자동 개발·승격 없음 | 격리된 변경 → 회귀 검사 → 제한된 적용 → 실패 시 복원 증거 |
| 외부 사용자와 사업 | 타깃/고객 수는 가설. 실제 사용자·매출 검증 없음 | 사용자별 데이터·권한 분리 후 3명 문제 검증 → 유료 1명 → 반복 사용자 10명 |

이번 변경은 `3757f86f`이며 전체 87개 검사 통과 후 기존 Koyeb에 배포했다. 암호화 백업·새 프로세스 복원과 분실 기기 차단을 확인했다. 자세한 검사·배포 범위는 `LIVE_ACCEPTANCE.md`를 따른다. 백업 파일과 복구 키의 지속 보관 전송은 실패했으므로 장기 백업 운영은 미완료다.

개발 작업자를 연결하기 전에 남은 요청 ID 문제를 처리한다. 프로젝트·자료 쓰기는 이미 ID를 검증한다. 명령·기기 등록 등 다른 공용 쓰기 경로는 ID를 생략할 수 있고, 공통 영수증은 최근 2,000개만 보관한다. 외부 파일 변경·결제·유료 실행의 장기 재시도 보장을 이 구현에 기대지 않는다.

기존 Codex·Claude 실행 파일/인증은 이 환경에서 확인되지 않았다. 이용 가능한 bwrap의 실제 격리 시작도 권한 오류로 실패했다. 이 제한을 없앤 척하거나, 코어 비밀을 가진 프로세스에서 임의 코드를 실행해 작업자 연결 완료로 처리하지 않는다. 코드와 검사 결과를 다음에 이어갈 수 있도록 보존한다.
