# BLACKHOLE 재개와 AI 작성 호출 경로 수정 — 2026-09-12

## 이번 결과

기준 소스 `1e235076f187748c02d132ffec214c4b2db96d73`의 178개 파일을 Git blob SHA·크기로 검증해 작업 사본을 복구했다. Node 24.19.0 기준선 검사 172/172 통과(19.831초). 실제 모델 연결 준비 중 옛 AI 설정과 준비된 primary 설정이 함께 있을 때 AI 작성이 호출 원장을 거치지 않는 결함을 재현하고 수정했다.

수정 전 합성 HTTP 재현: 하루 상한1, AI 작성2건 완료, legacy 호출2·primary 호출0·agent 원장 시도0. 전체 멈춤은409로 작동했다. 따라서 중단 자체의 우회가 아닌 호출 원장·일일 상한 경로의 불일치다.

수정 후 신규 `type=ai` 요청은 primary가 준비됐으면 항상 agent로 접수된다. 화면의 모델 표시도 같은 우선순위를 따른다. legacy만 있는 설치와 이전 작업의 종류/이력은 보존한다. 이전 legacy 작업의 재개를 새 agent 작업으로 자동 변환하지 않는다. legacy-only 방식 전체에 새로운 금액/호출 상한을 구현한 수정은 아니다.

## 실제 검사

- 새 회귀검사2개: 두 설정 공존, 정상 응답/응답 불명확, 원장 저장, 같은 요청 재사용, 프로세스 재시작, 하루 상한 거부, legacy fallback 미실행.
- 관련 제공자 검사6/6 통과.
- 최종 `npm test`: 174/174 통과, 실패·취소·생략0, 19.648초.
- 공급자는 합성 응답과 로컬 HTTP 서버다. 실제 모델 호출0, API 키 생성0, Secret 변경0, 신규 결제0, 운영 배포0, APK 빌드/설치0.

## 실제 연결 확인과 막힌 단계

OpenAI 로그인된 API Keys 페이지와 Koyeb Secrets 목록의 비밀값 없는 상태를 확인했다. Koyeb에는 `yeno-core-pairing`만 관찰됐다. 이 값은 앱 연결용이며 모델 API 키가 아니다. 공개 조종석 페이지는 로드됐지만 현재 탭은 연결 전이다. health URL 직접 탐색은 `ERR_BLOCKED_BY_CLIENT`여서 현재 인증 API·데이터 revision·운영 건강 상태는 새로 검증하지 못했다. 저장소 운영 브랜치는 `2b14d03f53cd1d03234c789e2cedf895be9546e3`로 유지된다.

자동 승인 검토에서 두 요청을 거부했다.
1. 키·결제 화면 전체 DOM 읽기: 비밀값/결제정보 노출 우려. 이후 비밀값을 반환하지 않는 제한된 상태 조회만 수행했다.
2. OpenAI `Create new secret key` 버튼: 새 API 접근 권한 생성에 대한 사용자 직접 승인이 확인되지 않음. 클릭/발급을 실행하지 않고 멈췄다. 다른 경로로 우회하지 않았다.

## 바로 이어갈 작업

사용자에게 OpenAI 전용 제한 권한 API 키 발급·기존 Koyeb Secret 연결·총 US$0.10 이내 첫 합성 시험을 구체적으로 승인받는 단계다. 결제수단 추가/잔액 충전/자동충전/구독 생성은 이 시험 범위에 포함하지 않는다. 키는 모델 요청에 필요한 권한만 부여하고 관리·파일·조직 권한을 부여하지 않는다. 실제 UI에서 더 넓은 권한만 선택 가능하면 조용히 확대하지 않는다.

첫 시험의 모델 후보는 `gpt-4.1-mini`다. 공식 문서에서 Chat Completions와 function calling 지원, 입력 US$0.40/출력 US$1.60(각100만 토큰)을 확인했다. 이는 계정의 실제 모델 접근 성공을 뜻하지 않는다. 공개/합성 입력의 작은 요청으로 검증하고, 전송 전에 입력·출력 상한으로 비용이 승인 범위 안인지 산정한다. 응답 불명확 시 재전송하지 않고 원장에 남긴다. 계정에 사용 가능한 잔액이 없으면 자동 구매하지 않는다.

- [모델·가격 공식 문서](https://developers.openai.com/api/docs/models/gpt-4.1-mini)
- [권한 공식 문서](https://developers.openai.com/api/docs/guides/rbac)

키 연결 시 기존 자동 검토 off·일일4회 상한을 확대하지 않는다. 기존 봇을 일괄 재개하지 않는다. 이번 코드 수정은 개발 브랜치에만 저장하며 실제 운영 반영·모델 호출·폰 명령/결과/종료/재접속은 각각 확인 후 기록한다. 원본49·신규1·보관20과 Obsidian Vault는 수정하지 않았다.
