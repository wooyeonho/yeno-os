{
  "name": "yeno-os-runtime",
  "version": "0.2.2",
  "private": true,
  "type": "module",
  "description": "YENO personal runtime: mobile control, durable jobs, memory and recovery",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node launch.mjs",
    "test": "node --test test/*.test.mjs"
  }
}
